function AreaofCircle(radius) {
    return Math.PI * radius * radius;
}

// const radius = 5;
// const area = AreaofCircle(radius);
// console.log(`The area of the circle is: ${area}`)

console.log(`The area of the circle is ${AreaofCircle(10)}`);